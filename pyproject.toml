[project]
name = "v2tts"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "faster-whisper",
  "numpy",
  "onnx-asr[cpu,hub]>=0.12,<0.13",
  "sounddevice",
  "soundfile",
]

[project.optional-dependencies]
build = [
  "pyinstaller",
]
